Im dritten Schritt soll Code erstellt werden, der die Verteilung der Transportzeiten nach Routentyp und Wetter in einem gruppierten Säulendiagramm (Barplot) visualisiert. Beginne mit einer kurzen, konzeptionellen Checkliste der Arbeitsschritte (3-7 Punkte), bevor du die eigentliche Umsetzung startest. Verwende als Eingabe einen Pandas DataFrame mit den Spalten: 'delivery_time_days', 'route_type' und 'weather_conditions'. Verwende als Ausgabe eine png-Datei. Stelle sicher, dass fehlende Werte (NaN) in diesen Spalten vor der Visualisierung angemessen behandelt werden, indem sie beispielsweise entfernt werden. Nach dem Erstellen des Diagramms, überprüfe in 1-2 Sätzen, ob die Visualisierung wie beabsichtigt dargestellt wird und ob alle Daten korrekt verarbeitet wurden; korrigiere gegebenenfalls. Das Diagramm soll für jeden Routentyp die Verteilung der Transportzeiten unter verschiedenen Wetterbedingungen zeigen.

# Hinweis
Warte anschließend bis der Nutzer den nächsten Schritt anfordert.